// Configuration for Google Cloud and other backend services
export const GCLOUD_PROJECT_ID = process.env.GCLOUD_PROJECT_ID || '';
export const GCLOUD_KEY_FILE = process.env.GCLOUD_KEY_FILE || '';

// Add other config values as needed
